let table = [
  { name: 1, budget: 3251 },
  { name: 2, budget: 3251 },
  { name: 2, budget: 32 },
  { name: 3, budget: 124 },
  { name: 1, budget: 3441 },
  { name: 5, budget: 6671 },
  { name: 4, budget: 321 },
  { name: 2, budget: 2251 },
  { name: 5, budget: 2241 },
  { name: 1, budget: 1231 }
];

class Tablet {
  constructor(uid,data) {
    this.name = uid;
    this.data = data;
  }
}

function unique(arr) {
  let result = [];

  for (let str of arr) {
    if (!result.includes(str.name)) {
      result.push(str.name);
    }
  }

  return result;
}

function sumOfData(arr) {
    let 
    for(let i = 0; i < arr.lenth; i++) {
        let arg = arr.name 
        
    }
}

let unicVal = unique(table);
let sumData = 12;

const createNewTable = unicVal.map((unicVal) => {
  return {
    name: unicVal,
    data: sumData
  };
});
console.log(createNewTable);

dsf
sdf
sdf
sdf
sdf
